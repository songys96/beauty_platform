import 'package:flutter/material.dart';

import 'components/body.dart';

class ShoppingMallScreen extends StatelessWidget {
  static String routeName = "/shopping_mall";

  @override
  Widget build(BuildContext context) {
    return Body();
  }
}
